# Web API
fastapi==0.110.2
uvicorn[standard]==0.30.1
aiofiles==23.2.1

# Templates
jinja2==3.1.3

# Scraping
beautifulsoup4==4.12.3
requests==2.31.0

# Multipart
python-multipart==0.0.9

# LlamaIndex (core + plugin de embeddings de HuggingFace)
llama-index==0.10.36
llama-index-embeddings-huggingface==0.2.3

# NLP
sentence-transformers==2.6.1     # ← subido para cumplir con el plugin
nltk==3.8.1
numpy==2.3.2                      # fijado para evitar intento de downgrade a 1.26 en Py3.13

# (opcional pero recomendado para evitar builds desde fuente)
torch==2.6.0                      # actualizado para compatibilidad con Python 3.13 (2.5.1 no disponible)
# torchvision es opcional; si no usas visión, no lo instales

# Lexical ranking adicional (BM25) - opcional para mejorar fallback sin embeddings
rank-bm25==0.2.2

# Utils
tqdm==4.66.4

# Testing
pytest==8.2.1

# OpenAI (opcional, para respuestas generativas restringidas al contexto)
openai>=1.40.0







