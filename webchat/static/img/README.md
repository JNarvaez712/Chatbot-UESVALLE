Place your chatbot avatar here as `uesly.png` (or replace references).

- Recommended: 320x320 PNG with transparent background.
- Path used by the widget UI: `/static/img/uesly.png`.
- After adding the file, refresh the page to see it in the header and next to bot messages.
